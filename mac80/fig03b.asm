TRUE	EQU 0FFFFH	;DEFINE "TRUE"
FALSE	EQU NOT TRUE	;DEFAULT "FALSE"
	IF	FALSE
	MVI	A,1
	IF	TRUE
	MVI	A,2
	ELSE
	MVI	A,3
	ENDIF
	MVI	A,4
	ELSE
	MVI	A,5
	IF	TRUE
	MVI	A,6
	ELSE
	MVI	A,7
	ENDIF
	MVI	A,8
	ENDIF
	END

